'use strict';

describe ("Test Students Controller", function () {

    //var stateparams, ctrl, httpBackend, group_id;
    //
    //beforeEach (angular.mock.module("app.admin.groups"));
    //beforeEach (angular.mock.inject(function($stateParams, $controller, _$httpBackend_, $state){
    //    httpBackend = _$httpBackend_;
    //    stateparams = $stateParams;
    //    group_id = stateparams.group_id;
    //    httpBackend.expectGet(_$httpBackend_).respond([{id: 1 }, {id: 2}, {id:3}, {id:4}, {id:5}, {id:6}, {id:7}, {id:8}, {id:9}, {id:10}])
    //    ctrl = $controller ("StudentsController");
    //}));
    it("Should show the list of students", function(){
        expect(true).toBe(true);
    });
});